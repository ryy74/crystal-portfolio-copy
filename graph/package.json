{
  "name": "crystal",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ crystal",
    "create-local": "graph create --node http://localhost:8020/ crystal",
    "remove-local": "graph remove --node http://localhost:8020/ crystal",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crystal",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.95.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.38.0",
    "matchstick-as": "0.6.0"
  }
}
